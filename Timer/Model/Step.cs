namespace Timer.model {
    public enum Step {
        DOWNLOAD,
        LOAD,
        EDIT,
        FREEZE_RELOAD,
        PAUSE,
        EXPORT,
        TOTAL,

        DOWNLOAD_LOAD,
        EDIT_LOAD_FREEZE,
        EDIT_FREEZE
    }
}
